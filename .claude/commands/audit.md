Your goal is to update any vulnerable depedencies.

Do the following:
1. Run `npm audit` to find vulnerable installed packages in this project.
2. Run `nmp audit fix` to apply audit fix.
3. Run tests and verify the updates didn't break everything.